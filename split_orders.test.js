import { findOrderGroups } from './split_orders';
import { all_orders } from './mock_json.js';

test('finds correct number of orders', () => {
    //Two orders after selected date
    expect(something.length).toBe(2);
});
